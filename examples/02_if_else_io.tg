# Lê dois inteiros e imprime o maior usando if/else (ramo/yro).
tekoha
  papy a, b, c.

  hei("Digite A").
  monee(a).
  hei("Digite B").
  monee(b).

  ramo (a < b) {
    c = b.
  } yro {
    c = a.
  }

  hei("Maior valor:").
  hei(c).
opa.
